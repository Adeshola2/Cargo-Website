# Cargo Website 🚢

This project is a cargo/shipping website built with **React + Vite**, providing a fast development experience with Hot Module Replacement (HMR) and ESLint.

## Tech Stack
- React
- Vite
- ESLint

## Available Plugins
Currently, two official Vite React plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) — uses Babel (or oxc) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) — uses SWC for Fast Refresh

## Setup

Install dependencies:
```bash
npm install
